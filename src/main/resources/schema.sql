CREATE TABLE Devices (DeviceID INT NOT NULL PRIMARY KEY,
                      DeviceModel VARCHAR(20) NOT NULL,
                      DeviceBrand VARCHAR(20) NOT NULL,
                      DeviceOS VARCHAR(20) NOT NULL,
                      DeviceOsVersion VARCHAR(20) NOT NULL);